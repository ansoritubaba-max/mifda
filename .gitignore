/.phpunit.cache
/node_modules
# CATATAN (deploy pipeline): /public/build SENGAJA gak di-ignore lagi.
# Hosting gak punya Node/npm buat build Vite otomatis di server, jadi
# alurnya: build lokal (`npm run build`) SEBELUM commit+push, hasil build
# ikut ke-commit ke git biar tinggal di-pull cPanel tanpa perlu build ulang.
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
